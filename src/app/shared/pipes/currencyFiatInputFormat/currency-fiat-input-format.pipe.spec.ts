import { CurrencyFiatInputFormatPipe } from './currency-fiat-input-format.pipe';

describe('CurrencyFiatInputFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyFiatInputFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
