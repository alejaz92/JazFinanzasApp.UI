import { CurrencyInvestmentFormatPipe } from './currency-investment-format.pipe';

describe('CurrencyInvestmentFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyInvestmentFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
