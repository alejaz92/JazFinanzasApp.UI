import { CommerceTypePipe } from './commerce-type.pipe';

describe('CommerceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CommerceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
